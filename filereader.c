#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char * readLine(FILE* file, char* buffer,size_t buffer_len){
    // char *line
    // while(fgets(buffer,sizeof(buffer), file) != NULL){

    // }
}